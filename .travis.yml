language: cpp
compiler:
  - gcc
before_install:
  - #sudo apt-get install libboost-all-dev libopencv-dev libcv-dev libopencv-features2d-dev libhighgui-dev zlib1g-dev libjpeg-dev libpng-dev libtiff-dev libjasper-dev libgtk2.0-dev
  - wget http://sourceforge.net/projects/opencvlibrary/files/opencv-unix/2.4.6.1/opencv-2.4.6.1.tar.gz/download?use_mirror=kent&use_mirror=kent
  - tar zxf opencv-2.4.6.1.tar.gz
  - cd opencv-2.4.6.1
  - mkdir build
  - cd build
  - mkdir ../../opencv
  - cmake .. -DCMAKE_INSTALL_PREFIX=../../opencv
  - make
  - make install
  - cd ../..
before_script:
  - mkdir build
  - cd build
  - cmake .. -DOpenCV_DIR=../opencv
script: 
  - make